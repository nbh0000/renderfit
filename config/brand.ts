/**
 * 서비스 브랜딩. 이름을 바꿀 일이 생기면 이 파일만 고치면 된다.
 * (상표·상호 검토 결과에 따라 결합상표 형태로 바꾸기 쉬우라고 분리해 둔다.)
 */
export const BRAND = {
  /** 화면에 노출되는 서비스명 */
  name: "렌더핏",
  /** 로고(워드마크)에 쓰는 영문 표기 — 별도 이미지 없이 글자만 쓴다 */
  wordmark: "RENDERFIT",
  /** 문서 타이틀 등에서 쓰는 전체 표기 */
  fullName: "렌더핏 RENDERFIT",
  /** 한 줄 소개 */
  tagline: "디자이너의 렌더 도구",
  /** 히어로 헤드라인 */
  headline: "사진 한 장, 도면 한 장이 완성된 시안이 됩니다",
  /** 워터마크에 찍히는 문자열 */
  watermark: "렌더핏",
  description:
    "사진·도면·스케치를 올리면 구조를 유지한 채 스타일 시안과 포토리얼 렌더를 만들어 주는 디자이너용 렌더 도구입니다.",
} as const;
